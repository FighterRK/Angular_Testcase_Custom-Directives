import { BodyDirDirective } from './body-dir.directive';

describe('BodyDirDirective', () => {
  it('should create an instance', () => {
    const directive = new BodyDirDirective();
    expect(directive).toBeTruthy();
  });
});
